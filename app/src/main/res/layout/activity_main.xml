<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main_layout"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.eddy.servr.Activities.MainActivity">

    <android.support.design.widget.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/AppTheme.AppBarOverlay">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/colorPrimary"
            app:subtitleTextColor="@color/black"

            android:textColor="@color/white"
            app:title="@string/app_title"
            app:titleTextColor="@color/white" />
        <!--

            app:popupTheme="@style/PopUpStyle"
            app:theme="@style/customToolbarStyle"

            android:popupTheme="@style/PopUpStyle"
            -->
        <android.support.design.widget.TabLayout
            android:id="@+id/MyTabs"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:theme="@style/WhiteLayoutStyle"
            app:tabGravity="center"
            app:tabIndicatorColor="@color/colorAccentRed2"
            app:tabSelectedTextColor="@color/colorAccentRed2"
            app:tabTextColor="@color/colorPrimary"

            />


    </android.support.design.widget.AppBarLayout>

    <include layout="@layout/content_main" />

</android.support.design.widget.CoordinatorLayout>
